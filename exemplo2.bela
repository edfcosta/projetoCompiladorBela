main() {
    var float n;
    var float i;
    var float soma;
    n = readInput();
    i = 0.0;
    soma = 0.0;

    while ((i < n)||(i==n)) then {
        soma = (soma + i);
        i= (i + 1);
    };
    printLn soma;
}